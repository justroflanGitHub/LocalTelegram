# Grafana Dashboard Provisioning

apiVersion: 1

providers:
  - name: 'LocalTelegram'
    orgId: 1
    folder: 'LocalTelegram'
    folderUid: 'localtelegram'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
